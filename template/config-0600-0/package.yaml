name: config
version: '0.0.0'

extra-source-files:
- package.yaml
- stack.yaml

ghc-options: -Wall

library:
  source-dirs: library
  dependencies:
  - base
  - hspec
  - hspec-discover
  - interpolate
  - text
